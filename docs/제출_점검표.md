# 병원동행 매칭 서비스 — 제출 점검표

- **코더**: 조경호
- **리뷰어**: 피하영

---

[O]  **1. 주어진 문제를 해결하는 완성된 코드가 제출되었나요? (완성도)**
- 문제에서 요구하는 최종 결과물이 첨부되었는지 확인
- 문제를 해결하는 완성된 코드란 프로젝트 루브릭 3개 중 2개, 퀘스트 문제 요구조건 등을 지칭
    - 해당 조건을 만족하는 부분의 코드 및 결과물을 캡쳐하여 사진으로 첨부

**근거 (코드)**

`src/lib/caregivers.ts` — 목록 조회 + 등록 기능이 Supabase에 실제로 연결됨:
```ts
export async function fetchCaregivers(): Promise<Caregiver[]> {
  const { data, error } = await supabase
    .from("caregivers")
    .select("*")
    .order("career_years", { ascending: false });
  if (error) throw error;
  return (data as CaregiverRow[]).map(toCaregiver);
}

export async function insertCaregiver(c: NewCaregiver): Promise<void> {
  const { error } = await supabase.from("caregivers").insert({
    name: c.name,
    is_certified: c.isCertified,
    career_years: c.careerYears,
    address: c.address,
    age_group: c.ageGroup,
    additional_certifications: c.additionalCertifications,
  });
  if (error) throw error;
}
```

라이브 배포 URL: https://nike1137-svg.github.io/hospital-app/ (`README.md:11`)

> ⚠️ **캡쳐 사진 미첨부**: "결과물을 캡쳐하여 사진으로 첨부"는 배포 사이트를 브라우저로 열어 스크린샷을 찍어야 하는데, 이번 점검에서는 캡쳐를 실제로 첨부하지 못했습니다. 코드 근거만으로 O로 판단했으며, 사진 근거가 필수라면 배포 사이트 방문 후 캡쳐 추가가 필요합니다.

---

[X]  **2. 체크리스트에 해당하는 항목들을 모두 수행하였나요? (문제 해결)**
- 아래 5개 세부 항목 중 4개는 O, 1개(로그인 데이터 권한 확인)는 X → **"모두 수행"은 아님**

    [O]  비밀값(키·비밀번호)을 코드에 직접 안 적고 **환경변수**로 뺐는가?

    **근거 (코드 + 문서 캡쳐)**
    ```ts
    // src/lib/supabaseClient.ts:3-6
    const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
    const supabaseKey = process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY;
    ```
    `docs/병원동행_보안점검.md:27-30` 실제 검증 명령:
    ```
    - git ls-files | grep .env → .env 추적 안 됨 ✅
    - git grep "sb_publishable|sb_secret|service_role" src → 소스에 키 하드코딩 없음 ✅
    ```

    ---

    [O]  가격·수량·권한 같은 **검증을 백엔드에서** 하는가? (프론트만 믿지 않기)

    **근거 (문서 캡쳐 — `docs/병원동행_API명세.md:35-48`)**
    ```
    | career_years | int | NOT NULL, default 0, >= 0 | careerYears |
    ```
    `docs/병원동행_API명세.md:64`:
    ```
    서버측 검증 = 위 NOT NULL·>= 0 제약 + RLS. 프론트 검증만 믿지 않는다. (루브릭 ① 대응)
    ```
    → DB 제약조건(`NOT NULL`, `>= 0`)으로 프론트 우회 입력을 막고 있음이 문서에 근거로 남아 있음.

    ---

    [O]  (Supabase를 쓴다면) **RLS를 켜고** 정책을 넣었는가?

    **근거 (문서 캡쳐 — `docs/병원동행_보안점검.md:36-51`, 라이브 DB 실측)**
    ```
    | 시도 | 기대 | 실제 결과 |
    |------|------|-----------|
    | caregivers 조회 | 허용 | ✅ 8명 조회됨 |
    | requests 조회 (연락처 등) | 차단 | ✅ 0건 — 개인정보 노출 안 됨 |
    | caregivers 삭제 | 차단 | ✅ 8→8명, 삭제 안 됨 |
    | caregivers 수정 | 차단 | ✅ 수정 반영 안 됨 |
    ```
    → "설정했다"가 아니라 공개 키로 직접 요청을 보내 차단/허용을 **실측**한 기록.

    ---

    [X]  로그인이 필요한 데이터에 **권한 확인(인가)**이 들어가는가?

    **근거 (문서 캡쳐 — 로그인 자체가 아직 없음)**
    `README.md:92` 개발 로드맵:
    ```
    - [ ] 회원가입·로그인 (공급자/수요자 구분)   ← Phase 2, 미완료
    ```
    `docs/병원동행_보안점검.md:69` 알려진 한계 1번:
    ```
    등록/요청 INSERT가 공개(anon) — 공개 폼이라 의도적이지만, 로그인·검증이 없어 스팸 입력이 가능.
    실서비스는 인증·검증(예: 자격 서류 확인, reCAPTCHA) 추가 필요.
    ```
    `docs/병원동행_보안점검.md:71` 알려진 한계 3번:
    ```
    연락처는 저장은 되나 조회는 막힘 — 실제 매칭 운영 시 관리자 전용 조회(인증된 role) 정책이 추가로 필요.
    ```
    → 현재 개인정보(연락처)는 "RLS로 아무도 못 읽게" 막는 방식이지, "로그인한 관리자만 읽게" 하는 **인가(authorization)** 방식이 아님. 로그인 기능 자체가 미구현 상태라 이 항목은 X.

    ---

    [O]  사용하는 무료 티어의 **한도와 요금 알림**을 아는가?

    **근거 (문서 캡쳐 — `docs/병원동행_보안점검.md:55-62`)**
    ```
    | 서비스 | 안전장치 |
    |--------|----------|
    | GitHub Pages | 과금 구조 없음. 무료 한도 초과 시 요금이 아니라 제한 |
    | Supabase | 무료 플랜, 신용카드 미등록 → 과금 자체가 불가능. 7일 미사용 시 일시정지(데이터 보존) |
    ```

---

[X]  **3. 프로젝트에 대한 회고가 상세히 기록 되어 있나요? (회고, 정리)**

    [X]  배운 점
    [X]  아쉬운 점
    [X]  느낀 점
    [X]  어려웠던 점

**근거**
```
$ ls docs/
병원동행_API명세.md
병원동행_보안점검.md
병원동행서비스_PRD.md
PRT_피어리뷰.md
```
→ 회고에 해당하는 파일·섹션을 저장소 전체에서 찾지 못함(4개 세부 항목 모두 미작성).

**제안**: `docs/회고.md`를 추가해 아래 틀을 채우는 것을 권장합니다.
```markdown
## 배운 점
## 아쉬운 점
## 느낀 점
## 어려웠던 점
```

---

## 종합
- 완성도: **O**
- 문제 해결 체크리스트: **X** (5개 중 4개 O, "로그인 데이터 권한 확인" 1개 X로 전체 미충족)
- 회고: **X** (전체 미작성)
